import numpy as np

def cross_correlation(image, filter, mode='valid'):
    
    return result_image

def convolution(image, filter, mode=’valid’):
    
    return np.flip(cross_correlation(image, filter, mode)) 